# Asset placeholders

Brand imagery for the Operations Manual. Shares the K Global brand with the
public website.

| File | Status | Notes |
|---|---|---|
| `logo.svg` | **Placeholder** | Text/SVG wordmark (shared with the public site). Replace with the real K Global logo. |
| `favicon.svg` | **Placeholder** | "K" + globe favicon. Replace with the real favicon. |

Brand colours live in `docs/stylesheets/extra.css` (single source of truth,
shared token set with the public website).
